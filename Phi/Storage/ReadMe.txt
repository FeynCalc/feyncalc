This file is here in order to have a non-empty storage directory.
If the directory is empty it cannot be in CVS and we are likely to
forget it when creating the distribution tarball.
Without the directory, CheckDB will not work properly.